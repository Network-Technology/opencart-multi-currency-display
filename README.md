# opencart-multi-currency-display
Multi-currency display extension for OpenCart. When the store's default currency is EUR, the module automatically shows the BGN equivalent next to every formatted EUR price. Lightweight OCMOD, no admin settings required.
